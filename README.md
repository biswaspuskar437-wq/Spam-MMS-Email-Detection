# Spam-MMS-Email-Detection
AI/ML-based Spam Email/MMS Detection system that identifies spam and legitimate messages in real time. It uses NLP, text preprocessing, TF-IDF feature extraction, and machine learning for classification. The system can analyze incoming emails and display spam detection results through an interactive Streamlit dashboard.
